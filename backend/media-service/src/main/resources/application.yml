server:
  port: 8084
  error:
    whitelabel:
      enabled: false

spring:
  application:
    name: media-service

minio:
  endpoint: http://localhost:9000
  access-key: minioadmin
  secret-key: minioadmin123
  bucket: grandma-stories
